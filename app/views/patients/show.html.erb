<ul>
    <% @patient.appointments.each do |a| %>
        <li><%= link_to a.appointment_datetime.strftime('%B %d, %Y at %k:%M'), appointment_path(a) %> - <%= link_to a.doctor.name, doctor_path(a.doctor) %></li>
    <% end %>
</ul>